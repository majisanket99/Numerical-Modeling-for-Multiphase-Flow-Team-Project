#!/usr/bin/gnuplot -persist

stats '../results/info.dat'
N  = STATS_mean_x
Nk = STATS_mean_y

set terminal wxt size 1366,768
# from xrandr | grep '*'

c=1
while (c==1) {
pause 0.06
#replot
#system("eval $(xdotool getmouselocation --shell); echo $X $Y > cursor.dat")
load "layout.plt"

}

# EOF
